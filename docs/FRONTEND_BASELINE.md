# Frontend Baseline

`frontend/` is the active app.

The UI baseline is locked. Preserve:
- left rail
- floating planner
- bottom route tray
- map overlay
- right journey panel

Do not change spacing, font, colors, or layout unless explicitly asked.

Build command:
```bash
cd frontend
npm run build
```
