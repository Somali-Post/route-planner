# AGENTS

- This is the active React/Vite frontend.
- Preserve the current Stitch-style overlay exactly.
- Do not redesign the UI unless explicitly asked.
- Do not change `App.css` spacing, colours, layout, or typography unless requested.
- Route planning must go through `src/services/journeyPlanner.js`.
- Do not import `buildMobilityOffers` directly into `App.jsx`.
- Run commands from `frontend/`.
- Build command: `npm run build`.
- Keep reports concise.
