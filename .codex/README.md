# Codex Project Config

Project-level Codex config can be added later in `.codex/config.toml` once the exact supported keys and schema are confirmed.

For now, no `config.toml` is created to avoid invalid or guessed configuration.
