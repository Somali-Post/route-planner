# AGENTS

- This is the isolated OTP2 backend workspace.
- Do not modify `frontend/` from router tasks unless explicitly asked.
- GTFS lives in `router/data/gtfs/`.
- OSM lives in `router/data/osm/`.
- OTP2 must work independently before frontend integration.
- Do not run Docker unless explicitly asked.
- Keep reports concise.
